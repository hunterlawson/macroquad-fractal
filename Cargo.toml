[package]
name = "macroquad-fractal"
version = "0.1.0"
edition = "2024"

[dependencies]
macroquad = "0.4.15"
strum = { version = "0.28.0", features = ["derive"] }

[profile.release]
lto = true
codegen-units = 1
